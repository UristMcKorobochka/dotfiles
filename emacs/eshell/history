git clone https://gitlab.com/protesilaos/dotfiles
sudo brightnessctl s 10%
acpi
git clean
git clean -f
git pull
cd emacs/.emacs.d/
cd ~
ls
cd projects/
ls
ls proj
ls ~/projects/
ls
cd ~/git/dotfiles/
cd emacs/.emacs.d/
git add prot-emacs.org 
git add prot-emacs.org 
git commit -m "Examples do have "src emacs-lisp" now. Just with a ":tangle no" property. Docu fix"
git commit -m "Examples do have "src emacs-lisp" now. Just with a ":tangle no" property. Docu fix"""
git commit -m "Examples do have 'src emacs-lisp' now. Just with a ':tangle no' property. Quick docu fix'"
date
cp -r * .config/emacs/ mygit/dotfiles/
